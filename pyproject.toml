[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "modulattice"
version = "0.1.0"
description = "Watch modules weave themselves into existence from pure potential."
readme = "README.md"
license = "MIT"
